[project]
name = "pystdf4"
version = "0.1.0"
description = "A Python library for reading and writing STDF4 files"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pystdf>=1.3.4",
    "pytest>=8.4.2",
]
